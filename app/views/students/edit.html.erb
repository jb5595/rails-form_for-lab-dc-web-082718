<h1>Student Form</h1>

<%= form_for(@student) do |f| %>
<label for="">First Name</label>

<%=f.text_field :first_name %>

<label for=""> Last Name</label>

<%= f.text_field :last_name %>

<%= f.submit :html=> {:id => "Create Student"} %>


<%end%>
